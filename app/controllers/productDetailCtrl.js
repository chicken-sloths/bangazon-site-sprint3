'use strict';

module.exports.displayProductDetail = (req, res, next) => {
  //Gets details for a product
  //Renders product-details.pug
};

module.exports.addToCart = (req, res, next) => {
  //Checks whether user is logged in
  //Redirects to login, if not?
  //Adds product to authed user's cart
  //Renders a notification that the product was successfully added
};
